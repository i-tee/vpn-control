# Порты вынесены в переменные, чтобы проект мог работать параллельно с
# другими локальными окружениями. Дефолты подобраны так, чтобы не пересекаться
# с checklist-expert (тот занимает 80, 3306, 1025, 8025).
#
# Переопределить можно через .env:
#   HTTP_PORT=8080  MAILPIT_UI_PORT=8125  MAILPIT_SMTP_PORT=1125  REDIS_PORT_HOST=6479
#
# Внутри docker-сети порты остаются стандартными (mailpit:1025, redis:6379),
# поэтому MAIL_PORT / REDIS_PORT в .env менять НЕ нужно — приложение ходит
# к контейнерам по именам, а не через хостовой проброс.

services:
    # Контейнер с приложением (PHP + Laravel)
    app:
        build: .
        container_name: gatekeeper-app
        restart: unless-stopped
        working_dir: /var/www/html
        volumes:
            - ./:/var/www/html
            - ./storage:/var/www/html/storage
            - ./bootstrap/cache:/var/www/html/bootstrap/cache
        depends_on:
            - redis

    # Контейнер с веб-сервером Nginx
    web:
        image: nginx:alpine
        container_name: gatekeeper-nginx
        restart: unless-stopped
        ports:
            # 8080 — на него смотрит ngrok, менять осторожно
            - "${HTTP_PORT:-8080}:80"
        volumes:
            - ./:/var/www/html
            - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
        depends_on:
            - app

    # Контейнер с Redis
    redis:
        image: redis:alpine
        container_name: gatekeeper-redis
        restart: unless-stopped
        ports:
            - "${REDIS_PORT_HOST:-6479}:6379"

    # 📧 Контейнер для тестирования почты (Mailpit)
    mailpit:
        image: axllent/mailpit:latest
        container_name: gatekeeper-mailpit
        restart: unless-stopped
        ports:
            - "${MAILPIT_UI_PORT:-8125}:8025"   # Веб-интерфейс (http://localhost:8125)
            - "${MAILPIT_SMTP_PORT:-1125}:1025" # SMTP с хоста
        environment:
            MP_SMTP_AUTH_ACCEPT_ANY: "true" # Принимать любой логин/пароль
            MP_SMTP_AUTH_ALLOW_INSECURE: "true" # Без шифрования (для локалки)
            MP_UI_MESSAGE_VIEWER: "true" # Показывать HTML-версию писем
